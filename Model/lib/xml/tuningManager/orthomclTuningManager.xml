<?xml version="1.0" encoding="UTF-8"?>
<tuningConfig>


  <tuningTable name="GroupAttributes">
    <externalDependency name="apidb.OrthologGroup"/>
    <externalDependency name="apidb.OrthologGroupAaSequence"/>
    <externalDependency name="apidb.Organism"/>
    <externalDependency name="dots.AaSequence"/>
    <internalDependency name="SequenceAttributes"/>

    <sql>
      <![CDATA[
create table GroupAttributes&1 as
SELECT
  og.group_id AS group_name,
  CASE
    WHEN og.is_residual = 1 THEN 'Residual'
    ELSE 'Core'
  END AS group_type,
  COALESCE(nm.number_of_members, 0) AS number_of_members,
  COALESCE(cm.number_of_core_members, 0) AS number_of_core_members,
  COALESCE(pm.number_of_peripheral_members, 0) AS number_of_peripheral_members,
  COALESCE(tc.core_taxa_count, 0) AS core_taxa_count,
  COALESCE(tc.peripheral_taxa_count, 0) AS peripheral_taxa_count,
  COALESCE(tc.taxa_count, 0) AS taxa_count
FROM apidb.orthologgroup og
LEFT JOIN (
  SELECT COUNT(aa_sequence_id) AS number_of_members, group_id
  FROM apidb.orthologgroupaasequence
  GROUP BY group_id
) nm 
  ON og.group_id = nm.group_id
LEFT JOIN (
  SELECT COUNT(core.aa_sequence_id) AS number_of_core_members, group_id
  FROM (
    SELECT ogas.aa_sequence_id, ogas.group_id
    FROM apidb.organism og
    JOIN dots.aasequence das 
      ON das.taxon_id = og.taxon_id
    JOIN apidb.orthologgroupaasequence ogas 
      ON ogas.aa_sequence_id = das.aa_sequence_id
    WHERE og.core_peripheral = 'core'
  ) AS core
  GROUP BY group_id
) cm 
  ON og.group_id = cm.group_id
LEFT JOIN (
  SELECT COUNT(peripheral.aa_sequence_id) AS number_of_peripheral_members, group_id
  FROM (
    SELECT ogas.aa_sequence_id, ogas.group_id
    FROM apidb.organism og
    JOIN dots.aasequence das 
      ON das.taxon_id = og.taxon_id
    JOIN apidb.orthologgroupaasequence ogas 
      ON ogas.aa_sequence_id = das.aa_sequence_id
    WHERE og.core_peripheral = 'peripheral'
  ) AS peripheral
  GROUP BY group_id
) pm
  ON og.group_id = pm.group_id
LEFT JOIN (
  SELECT
    group_name,
    COUNT(DISTINCT CASE WHEN core_peripheral = 'C' THEN taxon_abbreviation END) AS core_taxa_count,
    COUNT(DISTINCT CASE WHEN core_peripheral = 'P' THEN taxon_abbreviation END) AS peripheral_taxa_count,
    COUNT(DISTINCT taxon_abbreviation) AS taxa_count
  FROM SequenceAttributes
  WHERE core_peripheral IN ('C', 'P')
  GROUP BY group_name
) tc
  ON og.group_id = tc.group_name
      ]]>
    </sql>

    <sql>
      <![CDATA[
        alter table GroupAttributes&1
          add constraint GrpAttrs_pk&1 primary key (group_name)
      ]]>
    </sql>
  </tuningTable>

  <tuningTable name="SequenceAttributes">
    <externalDependency name="apidb.OrthologGroup"/>
    <externalDependency name="apidb.OrthologGroupAaSequence"/>
    <externalDependency name="apidb.OrthomclClade"/>
    <externalDependency name="apidb.OrthomclResource"/>
    <externalDependency name="apidb.Organism"/>
    <externalDependency name="dots.AaSequenceEnzymeClass"/>
    <externalDependency name="dots.AaSequence"/>
    <internalDependency name="TranscriptAttributes"/>    
    <internalDependency name="DomainAssignment"/>

    <sql>
      <!-- TODO:  urls and lenght are broken -->
      <![CDATA[
CREATE TABLE SequenceAttributes&1 AS
SELECT
    aas.source_id AS full_id,
    aas.source_id,
    aas.aa_sequence_id,
    length(aas.sequence) AS length,

    COALESCE(aas.description, ta.gene_product) AS product,

    aas.taxon_id,

    taxon.orthomcl_taxon_id,
    taxon.taxon_group,
    taxon.orthomcl_abbrev AS taxon_abbreviation,
    taxon.name AS organism_name,
    taxon.core_peripheral,

    o.group_id AS group_name,
    o.ortholog_group_id,
    o.number_of_members AS group_size,
    COALESCE(cm.number_of_core_members, 0) AS number_of_core_members,
    COALESCE(pm.number_of_peripheral_members, 0) AS number_of_peripheral_members,
    CASE o.is_residual WHEN 1 THEN 'Residual'
         ELSE 'Core' END AS group_type,

    urls.source_url,
    urls.source_text,
    da_agg.pfam_domains

FROM dots.AASequence aas

LEFT JOIN (
    SELECT
        aa_sequence_id,
        string_agg(accession, ', ') AS pfam_domains
    FROM DomainAssignment
    GROUP BY aa_sequence_id
) da_agg
  ON aas.aa_sequence_id = da_agg.aa_sequence_id

JOIN apidb.orthologGroupAASequence ogseq
  ON ogseq.aa_sequence_id = aas.aa_sequence_id

JOIN apidb.orthologGroup o
  ON o.group_id = ogseq.group_id

LEFT JOIN (
    SELECT COUNT(ogas.aa_sequence_id) AS number_of_core_members, ogas.group_id
    FROM apidb.organism org_c
    JOIN dots.aasequence das_c
      ON das_c.taxon_id = org_c.taxon_id
    JOIN apidb.orthologgroupaasequence ogas
      ON ogas.aa_sequence_id = das_c.aa_sequence_id
    WHERE org_c.core_peripheral = 'core'
    GROUP BY ogas.group_id
) cm
  ON o.group_id = cm.group_id

LEFT JOIN (
    SELECT COUNT(ogas.aa_sequence_id) AS number_of_peripheral_members, ogas.group_id
    FROM apidb.organism org_p
    JOIN dots.aasequence das_p
      ON das_p.taxon_id = org_p.taxon_id
    JOIN apidb.orthologgroupaasequence ogas
      ON ogas.aa_sequence_id = das_p.aa_sequence_id
    WHERE org_p.core_peripheral = 'peripheral'
    GROUP BY ogas.group_id
) pm
  ON o.group_id = pm.group_id

JOIN (
    SELECT
        o.orthomcl_abbrev,
        o.taxon_id AS orthomcl_taxon_id,
        t.name,
        t.core_peripheral,
        t.taxon_group
    FROM apidb.organism o
    JOIN (
        WITH RECURSIVE TaxonHierarchy AS (
            SELECT
                three_letter_abbrev,
                orthomcl_clade_id,
                name,
                core_peripheral,
                name AS taxon_group,
                parent_id
            FROM apidb.OrthomclClade
            WHERE name IN (
                'Archaea','Bacteria','Alveolates','Amoebozoa',
                'Euglenozoa','Fungi','Metazoa',
                'Other Eukaryota','Viridiplantae'
            )

            UNION ALL

            SELECT
                child.three_letter_abbrev,
                child.orthomcl_clade_id,
                child.name,
                child.core_peripheral,
                parent.taxon_group,
                child.parent_id
            FROM apidb.OrthomclClade child
            JOIN TaxonHierarchy parent
              ON child.parent_id = parent.orthomcl_clade_id
        )
        SELECT three_letter_abbrev, taxon_group, name, core_peripheral
        FROM TaxonHierarchy
        WHERE core_peripheral IN ('C','P')
    ) t
      ON t.three_letter_abbrev = o.orthomcl_abbrev
) taxon
  ON taxon.orthomcl_taxon_id = aas.taxon_id

JOIN (
    SELECT
        aas.aa_sequence_id,
        CASE
          WHEN ores.resource_name IN (
            'AmoebaDB','CryptoDB','FungiDB','GiardiaDB','HostDB',
            'MicrosporidiaDB','PlasmoDB','PiroplasmaDB','ToxoDB',
            'TrichDB','TriTrypDB','VectorBase'
          )
          THEN SUBSTR(ores.resource_url,0,strpos(ores.resource_url,'/downloads'))
               || 'record/gene/' || aas.source_id

          WHEN lower(ores.resource_name) = 'uniprot'
          THEN REPLACE(ores.resource_url, 'EXTERNAL_ID_HERE', aas.source_id)
          ELSE ''
        END AS source_url,

        CASE
          WHEN ores.resource_name IS NULL THEN ''
          ELSE aas.source_id || ' (' || ores.resource_name || ')'
        END AS source_text

    FROM dots.AASequence aas
    JOIN apidb.organism ot
      ON ot.taxon_id = aas.taxon_id
    JOIN apidb.orthomclresource ores
      ON ot.taxon_id = ores.orthomcl_taxon_id
) urls
  ON aas.aa_sequence_id = urls.aa_sequence_id

LEFT JOIN transcriptattributes ta
  ON ta.protein_source_id = aas.source_id

WHERE aas.taxon_id IN (
    SELECT DISTINCT eas.taxon_id
    FROM apidb.organism og
    JOIN dots.AASequence eas
      ON eas.taxon_id = og.taxon_id
)
      ]]>
    </sql>

    <sql>
      <![CDATA[
        alter table SequenceAttributes&1
          add constraint SeqAttrs_pk&1 primary key (full_id)
      ]]>
    </sql>

    <sql>
      <![CDATA[
        create unique index SeqAttrs_idx&1 ON SequenceAttributes&1 (full_id, group_name, taxon_id, source_id)
      ]]>
    </sql>

    <sql>
      <![CDATA[
        create unique index SeqAttrs_gusIdx&1 ON SequenceAttributes&1 (ortholog_group_id, aa_sequence_id)
      ]]>
    </sql>

    <sql>
      <![CDATA[
        create unique index SeqAttrs_idx2&1 ON SequenceAttributes&1 (group_name, length desc, full_id, taxon_id)
      ]]>
    </sql>

    <sql>
      <![CDATA[
        create unique index SeqAttrs_idx3&1
          on SequenceAttributes&1 (aa_sequence_id, group_name, ortholog_group_id, orthomcl_taxon_id, taxon_id)
      ]]>
    </sql>

    <sql>
      <![CDATA[
        create unique index SeqAttrs_idx4&1 ON SequenceAttributes&1 (source_id, full_id, group_name, taxon_id)
      ]]>
    </sql>

  </tuningTable>

  <tuningTable name="GroupStatsWide">
    <externalDependency name="apidb.OrthologGroup"/>
    <externalDependency name="apidb.orthologgroupstats"/>

    <sql>
      <![CDATA[
create table GroupStatsWide&1 (
    group_name TEXT PRIMARY KEY,
    -- Core statistics (protein_subset = 'C')
    core_min_evalue TEXT,
    core_25_evalue TEXT,
    core_median_evalue TEXT,
    core_75_evalue TEXT,
    core_max_evalue TEXT,
    -- Core + Peripheral statistics (protein_subset = 'C+P')
    core_peripheral_min_evalue TEXT,
    core_peripheral_25_evalue TEXT,
    core_peripheral_median_evalue TEXT,
    core_peripheral_75_evalue TEXT,
    core_peripheral_max_evalue TEXT,
    -- Residual statistics (protein_subset = 'R')
    residual_min_evalue TEXT,
    residual_25_evalue TEXT,
    residual_median_evalue TEXT,
    residual_75_evalue TEXT,
    residual_max_evalue TEXT
)
      ]]>
    </sql>

    <sql>
      <![CDATA[
INSERT INTO groupstatswide&1
SELECT
    og.group_id AS group_name,
    -- Core statistics
    MAX(CASE WHEN ogs.protein_subset = 'C' AND ogs.stat_type = 'min' THEN ogs.evalue END) AS core_min_evalue,
    MAX(CASE WHEN ogs.protein_subset = 'C' AND ogs.stat_type = '25 PCT' THEN ogs.evalue END) AS core_25_evalue,
    MAX(CASE WHEN ogs.protein_subset = 'C' AND ogs.stat_type = 'median' THEN ogs.evalue END) AS core_median_evalue,
    MAX(CASE WHEN ogs.protein_subset = 'C' AND ogs.stat_type = '75 PCT' THEN ogs.evalue END) AS core_75_evalue,
    MAX(CASE WHEN ogs.protein_subset = 'C' AND ogs.stat_type = 'max' THEN ogs.evalue END) AS core_max_evalue,
    -- Core + Peripheral statistics
    MAX(CASE WHEN ogs.protein_subset = 'C+P' AND ogs.stat_type = 'min' THEN ogs.evalue END) AS core_peripheral_min_evalue,
    MAX(CASE WHEN ogs.protein_subset = 'C+P' AND ogs.stat_type = '25 PCT' THEN ogs.evalue END) AS core_peripheral_25_evalue,
    MAX(CASE WHEN ogs.protein_subset = 'C+P' AND ogs.stat_type = 'median' THEN ogs.evalue END) AS core_peripheral_median_evalue,
    MAX(CASE WHEN ogs.protein_subset = 'C+P' AND ogs.stat_type = '75 PCT' THEN ogs.evalue END) AS core_peripheral_75_evalue,
    MAX(CASE WHEN ogs.protein_subset = 'C+P' AND ogs.stat_type = 'max' THEN ogs.evalue END) AS core_peripheral_max_evalue,
    -- Residual statistics
    MAX(CASE WHEN ogs.protein_subset = 'R' AND ogs.stat_type = 'min' THEN ogs.evalue END) AS residual_min_evalue,
    MAX(CASE WHEN ogs.protein_subset = 'R' AND ogs.stat_type = '25 PCT' THEN ogs.evalue END) AS residual_25_evalue,
    MAX(CASE WHEN ogs.protein_subset = 'R' AND ogs.stat_type = 'median' THEN ogs.evalue END) AS residual_median_evalue,
    MAX(CASE WHEN ogs.protein_subset = 'R' AND ogs.stat_type = '75 PCT' THEN ogs.evalue END) AS residual_75_evalue,
    MAX(CASE WHEN ogs.protein_subset = 'R' AND ogs.stat_type = 'max' THEN ogs.evalue END) AS residual_max_evalue
FROM apidb.orthologgroup og
LEFT JOIN apidb.orthologgroupstats ogs ON og.group_id = ogs.group_id
GROUP BY og.group_id
      ]]>
    </sql>
  </tuningTable>

  <tuningTable name="GroupProteinPfam">
    <internalDependency name="SequenceAttributes"/>
    <internalDependency name="DomainAssignment"/>
    <sql>
      <![CDATA[
   CREATE TABLE GroupProteinPfam&1 as
   SELECT sa.group_name, 
       sa.full_id, 
       ROUND(sa.length::numeric, 0) AS protein_length, 
       sa.organism_name AS taxon_name, 
       CASE 
           WHEN sa.core_peripheral = 'C' THEN 'Core' 
           WHEN sa.core_peripheral = 'P' THEN 'Peripheral' 
           ELSE '' 
       END AS core_peripheral, 
       da.accession, 
       da.domain_index, 
       da.start_min, 
       da.end_max
FROM SequenceAttributes sa
LEFT OUTER JOIN DomainAssignment da 
    ON da.full_id = sa.full_id
ORDER BY sa.full_id, da.start_min
      ]]>
    </sql>

    <sql>
      <![CDATA[
   create index grpprotpfam_1&1 on GroupProteinPfam&1 (group_name)
      ]]>
    </sql>
  </tuningTable>
  
  <tuningTable name="SimilarOrthologGroup">
    <externalDependency name="apidb.OrthologGroup"/>
    <externalDependency name="apidb.OrthologGroupAaSequence"/>
    <externalDependency name="apidb.SimilarOrthologGroup"/>
    <externalDependency name="apidb.OrthomclGroupKeyword"/>
    <internalDependency name="DomainAssignment"/>    

    <sql>
      <![CDATA[
CREATE TABLE SimilarOrthologGroup&1 as
SELECT 
    og.group_id AS group_name, 
    sog.similar_group_id AS similar_group,  
    groupProteinCount.protein_count AS protein_count, 
    verbiage.keywords AS keyword, 
    domains.pfam, 
    evalue
FROM 
    apidb.SimilarOrthologGroup sog
JOIN 
    apidb.orthologgroup og ON og.group_id = sog.group_id
JOIN 
    (SELECT 
        ogas.group_id, 
        COUNT(ogas.aa_sequence_id) AS protein_count
     FROM 
        apidb.orthologgroupaasequence ogas
     GROUP BY 
        ogas.group_id) groupProteinCount ON sog.similar_group_id = groupProteinCount.group_id
JOIN 
    (SELECT 
        ortholog_group_id, 
        string_agg(keyword, '; ' ORDER BY CAST(frequency AS NUMERIC) DESC) AS keywords
     FROM 
        apidb.OrthomclGroupKeyword
     GROUP BY 
        ortholog_group_id) verbiage ON sog.similar_group_id = verbiage.ortholog_group_id
JOIN 
    (SELECT 
        group_name, 
        string_agg(DISTINCT accession, ', ') AS pfam
     FROM 
        DomainAssignment
     GROUP BY 
        group_name) domains ON sog.similar_group_id = domains.group_name
ORDER BY 
    evalue ASC
      ]]>
    </sql>

    <sql>
      <![CDATA[
   create index simorthgrp_1&1 on SimilarOrthologGroup&1 (group_name)
      ]]>
    </sql>
  </tuningTable>

  <!-- TODO:  if this is needed, need to redo the EC_NUMBER query -->
  <tuningTable name="TypeAheadCounts">
    <externalDependency name="dots.DbRefAaFeature"/>
    <externalDependency name="sres.DbRef"/>
    <externalDependency name="dots.AaFeature"/>
    <externalDependency name="sres.ExternalDatabase"/>
    <externalDependency name="sres.ExternalDatabaseRelease"/>
    <externalDependency name="sres.EnzymeClass"/>
    <externalDependency name="dots.AaSequenceEnzymeClass"/>
    <externalDependency name="dots.GoAssociation"/>
    <externalDependency name="dots.GoAssociationInstance"/>
    <externalDependency name="sres.OntologyTerm"/>
    <externalDependency name="core.TableInfo"/>
    <sql>
      <![CDATA[
CREATE TABLE TypeAheadCounts&1 AS
   (SELECT DISTINCT dr.primary_identifier as option_id,
                   count(distinct aaf.aa_sequence_id) AS protein_count
   FROM dots.DbRefAaFeature draf, sres.DbRef dr, dots.aafeature aaf,
        sres.ExternalDatabaseRelease edr, sres.ExternalDatabase ed
   WHERE draf.db_ref_id = dr.db_ref_id
   AND dr.external_database_release_id
       = edr.external_database_release_id
   AND edr.external_database_id = ed.external_database_id
   AND aaf.aa_feature_id = draf.aa_feature_id
   GROUP BY dr.primary_identifier
   )
   UNION
   (SELECT DISTINCT ec.ec_number AS option_id,
          COUNT(DISTINCT asec.aa_sequence_id) AS protein_count
   FROM sres.enzymeClass ec,
        dots.aaSequenceEnzymeClass asec
   WHERE  asec.enzyme_class_id = ec.enzyme_class_id
   GROUP BY ec.ec_number)
   UNION                
   (SELECT DISTINCT gt.source_id AS option_id,
           COUNT(DISTINCT aaf.aa_sequence_id) AS protein_count
    FROM dots.AaFeature aaf,
         dots.GoAssociation ga, sres.OntologyTerm gt,
         dots.GoAssociationInstance gai,core.TableInfo ti
    WHERE aaf.aa_sequence_id = ga.row_id
    AND ga.table_id = ti.table_id
    AND ti.name = 'ExternalAASequence'
    AND ga.go_term_id = gt.ontology_term_id
    AND ga.go_association_id = gai.go_association_id
    GROUP BY gt.source_id)
      ]]>
    </sql>
    <sql>
      <![CDATA[
CREATE INDEX TypeAheadCounts_idx&1 ON TypeAheadCounts&1 (option_id)
      ]]>
    </sql>
  </tuningTable>

  <tuningTable name="DomainAssignment">
    <externalDependency name="apidb.OrthologGroup"/>
    <externalDependency name="apidb.OrthologGroupAaSequence"/>
    <externalDependency name="dots.AaSequence"/>
    <externalDependency name="apidb.interproresults"/>
    <intermediateTable name="DomainIndex"/>
    <sql>
      <![CDATA[
        create table DomainAssignment&1 as
        select aas.source_id as full_id,
               o.group_id as group_name,
               r.interpro_primary_id as accession,
               r.interpro_desc as description,
               CAST (NULL as NUMERIC) as domain_index,
               aas.aa_sequence_id,
               r.interpro_start_min as start_min,
               r.interpro_end_min as end_max
        from dots.AASequence aas,
             apidb.orthologGroup o,
             apidb.orthologGroupAASequence ogseq,
             apidb.interproresults r
        where aas.aa_sequence_id = ogseq.aa_sequence_id
          and ogseq.group_id = o.group_id
          and aas.source_id = r.protein_source_id
          and upper(r.interpro_db_name) = 'PFAM'
      ]]>
    </sql>

    <sql>
      <![CDATA[
        create index domain_accession_ix&1
               on DomainAssignment&1 (accession, full_id, group_name)
      ]]>
    </sql>

    <sql>
      <![CDATA[
        create table domainIndex as
        select row_number() OVER () as domain_index, accession
        from (select distinct accession
              from DomainAssignment&1
              order by accession)
      ]]>
    </sql>

    <sql>
      <![CDATA[
        create index domainIdxIdx on DomainIndex(accession, domain_index)
      ]]>
    </sql>

    <sql>
      <![CDATA[
        update DomainAssignment&1 da
        set domain_index = (select domain_index
                            from DomainIndex
                            where accession = da.accession)
      ]]>
    </sql>

    <sql>
      <![CDATA[
        create index domain_ix_ix&1
               on DomainAssignment&1 (domain_index, accession, full_id)
      ]]>
    </sql>

    <sql>
      <![CDATA[
        create index domain_group_ix&1
               on DomainAssignment&1 (group_name, accession, full_id)
      ]]>
    </sql>

    <sql>
      <![CDATA[
        create index domain_seq_ix&1
               on DomainAssignment&1 (aa_sequence_id, accession, full_id, group_name)
      ]]>
    </sql>

  </tuningTable>

  <tuningTable name="PhyleticPattern">
    <comment>
      Mirrors webready.PhyleticPattern exactly (same 102 actual/total/percent
      columns, verified column-for-column and in the same order) -- read
      straight from it instead of independently re-deriving clade totals
      from apidb.orthologgrouptaxon/apidb.OrthomclClade, which is what this
      table used to do. Also picks up webready.PhyleticPattern's
      divide-by-zero guard on the percent columns, which this table
      previously lacked.

      TEMPORARY: the externalDependency elements below target
      orthologgrouptaxon/orthologgroup (what webready.PhyleticPattern is
      itself built from) instead of webready.PhyleticPattern directly,
      because that table has no modification_date column and the tuning
      manager's freshness check (which runs a max(modification_date) query
      against every declared dependency) errors out on it. Once
      PhyleticPattern.psql adds a modification_date column (matching the
      convention used by most other webready psql scripts), switch the
      externalDependency element back to name="webready.PhyleticPattern".
    </comment>
    <externalDependency name="ApiDB.OrthologGroupTaxon"/>
    <externalDependency name="apidb.OrthologGroup"/>

    <sql>
      <![CDATA[
CREATE TABLE PhyleticPattern&1 AS
SELECT * FROM webready.PhyleticPattern
      ]]>
    </sql>

    <sql>
      <![CDATA[
CREATE INDEX PhyleticPattern_idx&1 ON PhyleticPattern&1 (group_name)
      ]]>
    </sql>
  </tuningTable>

  <tuningTable name="GroupDomainAttribute">
    <externalDependency name="apidb.OrthologGroup"/>
    <internalDependency name="DomainAssignment"/>
    <sql>
      <![CDATA[
CREATE TABLE GroupDomainAttribute&1 AS
(
SELECT og.group_id AS group_name, ag.descriptions
FROM apidb.OrthologGroup og,
     (SELECT group_name,
             STRING_AGG(accession ||' (' || num_proteins|| ')', ', ')  AS descriptions
      FROM (SELECT group_name, accession, num_proteins, rnk
            FROM (SELECT group_name, accession, num_proteins,
	                 rank() OVER (PARTITION BY group_name ORDER BY num_proteins DESC) rnk
		  FROM (SELECT group_name, accession, count(distinct full_id) AS num_proteins
		        FROM DomainAssignment
			GROUP BY group_name,accession
		        )
		  )
	    WHERE rnk <= 3
	    )
      GROUP BY group_name
      ORDER BY 1
      ) ag
WHERE  og.group_id = ag.group_name
)
      ]]>
    </sql>
    <sql>
      <![CDATA[
CREATE INDEX GroupDomainAttribute_idx&1 ON GroupDomainAttribute&1 (group_name)
      ]]>
    </sql>
  </tuningTable>



  <tuningTable name="SequenceEnzymeClass">
    <externalDependency name="dots.AASequence"/>
    <externalDependency name="dots.AASequenceEnzymeClass"/>
    <externalDependency name="sres.enzymeclass"/>
    <internalDependency name="SequenceAttributes"/>
    <sql>
      <![CDATA[
      CREATE TABLE SequenceEnzymeClass&1 AS (
        SELECT sa.full_id
             , sa.group_name
             -- , sec.uniprot_accession
             , ec.ec_number
             , ec.description
             , ec.parent_id as ec_parent
             , ec.ec_number_1
             , ec.ec_number_2
             , ec.ec_number_3
             , ec.ec_number_4
        FROM sequenceattributes sa
           , dots.AASequence aa
           , (select distinct aa_sequence_id, enzyme_class_id from dots.AASequenceEnzymeClass) sec
           , sres.ENZYMECLASS ec
        WHERE sa.aa_sequence_id = aa.aa_sequence_id
        AND sec.aa_sequence_id  = aa.aa_sequence_id
        AND sec.enzyme_class_id = ec.enzyme_class_id
      )
      ]]>
    </sql>
    <sql>
      <![CDATA[
        CREATE INDEX SequenceEnzymeClass_idx1&1 ON SequenceEnzymeClass&1 (group_name, ec_number, description)
      ]]>
    </sql>
    <sql>
      <![CDATA[
        CREATE INDEX SequenceEnzymeClass_idx2&1 ON SequenceEnzymeClass&1 (full_id, ec_number, description)
      ]]>
    </sql>

  </tuningTable>

  <tuningTable name="GroupDomainFrequency">
    <externalDependency name="apidb.OrthologGroupAaSequence"/>
    <externalDependency name="dots.AaSequence"/>
    <externalDependency name="apidb.interproresults"/>
    <sql>
      <![CDATA[
        create table GroupDomainFrequency&1 as
        SELECT
            ogas.group_id AS group_name,
            ir.interpro_desc AS keyword,
            COUNT(DISTINCT ogas.aa_sequence_id)::float / grp.seq_count AS frequency
        FROM apidb.orthologgroupaasequence ogas
        INNER JOIN dots.aasequence aas
            ON ogas.aa_sequence_id = aas.aa_sequence_id
        INNER JOIN apidb.interproresults ir
            ON aas.source_id = ir.protein_source_id
            AND ir.interpro_db_name = 'Pfam'
        INNER JOIN (
            SELECT group_id, COUNT(*) AS seq_count
            FROM apidb.orthologgroupaasequence
            GROUP BY group_id
        ) grp ON ogas.group_id = grp.group_id
        GROUP BY ogas.group_id, ir.interpro_desc, grp.seq_count
      ]]>
    </sql>

    <sql>
      <![CDATA[
        create index group_domain_accession_ix&1
               on GroupDomainFrequency&1 (group_name)
      ]]>
    </sql>

  </tuningTable>

</tuningConfig>
